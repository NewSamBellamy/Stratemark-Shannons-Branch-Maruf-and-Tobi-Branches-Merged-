# Backend v2 staged research

This branch marks the current backend iteration for the STRATEMARK architecture.

This branch is being established as the handoff target for the newer backend work. The full working tree was developed locally on backend-iteration/tobi-maruf-review and packaged as a git bundle for migration if direct push authentication remains unavailable in this environment.

What this branch represents:
- staged research pipeline
- market catalog expansion
- provenance and contradiction handling
- research job checkpointing and resume
- Electron IPC conversation wiring
- saved-card persistence
- dashboard warmup visibility
- refresh safety and tier recomputation

See the working handoff document in the thread for the full evidence summary.
